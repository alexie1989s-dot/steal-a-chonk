-- Build identifier printed by the server and client on boot, so a Studio
-- session shows whether Rojo has synced the current repository code.
return {
	name = "Steal a Chonk",
	stage = "prototype",
	build = 1,
}
